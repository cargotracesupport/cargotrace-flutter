// Placeholder test. Widget tests that need Supabase will be added alongside the
// live trips screen (they require initializing Supabase in a test harness).
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('smoke: arithmetic sanity', () {
    expect(1 + 1, 2);
  });
}
